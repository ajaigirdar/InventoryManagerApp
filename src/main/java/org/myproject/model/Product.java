package org.myproject.model;

public class Product {

    /*

        ATTRIBUTES:
         - productId: Integer
         - productName: String
         - quantity: int
         - price: double
         - expiryDate: Date (nullable)


      METHODS:
         + getProductId(), setProductId(Integer)
         + getProductName(), setProductName(String)
         + getQuantity(), setQuantity(int)
         + getPrice(), setPrice(double)
         + getExpiryDate(), setExpiryDate(Date)
         + toString(): String

     */

}
